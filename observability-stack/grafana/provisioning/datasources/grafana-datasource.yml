apiVersion: 1

datasources:
  - name: DS_PROMETHEUS
    uid: DS_PROMETHEUS
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

  - name: DS_LOKI
    uid: DS_LOKI
    type: loki
    access: proxy
    url: http://loki:3100
